#pragma once

#include "nickelnote.h"

/*
class NickelNote : public QWidget {
public:
    NickelNote(const QString &filePath, QWidget *parent);

private:
    QString readHtmlFromFile(const QString &filePath);
};
*/